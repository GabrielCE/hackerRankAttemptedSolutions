#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    int x;
    int y;
    int temp;
    
    cin >> x >> y;
    
    int array [x];
    
    for(int i = 0; i < x; i++){
        cin >> array[i];
    }
    
    for(int i = 0; i < x; i++){
        temp = (i + y)%x;
        cout << array[temp] << " ";
    }
    
    return 0;
}
